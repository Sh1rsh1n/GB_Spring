spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/private?currentSchema=NOTE_DB
    username: postgres
    password: 1234qwer
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update